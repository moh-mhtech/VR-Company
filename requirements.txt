# Runtime dependencies for the VR-Company AutoGen experiment.
# Pin AutoGen 0.7.x — AgentOps may otherwise pull older AgentChat builds.
autogen-agentchat>=0.7.5,<0.8
autogen-ext[openai]>=0.7.5,<0.8
autogen-core>=0.7.5,<0.8
pyyaml>=6.0
python-dotenv>=1.0
agentops>=0.4.0
pytest>=8.0
